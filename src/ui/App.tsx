// Top-level shell. Routes between the character list, the wizard, and the editor.
// Empty stub for now — implementations land alongside the engine.

import type { JSX } from "react";

export function App(): JSX.Element {
  return (
    <main style={{ padding: "1rem" }}>
      <h1>MegaTraveller Character Generator</h1>
      <p>Scaffold up. Engine and UI to follow.</p>
    </main>
  );
}
